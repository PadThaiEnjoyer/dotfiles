return {
    {
        "easymotion/vim-easymotion",
        config = function()
            -- Optional: Customize the prefix (default is <Leader><Leader>)
            -- vim.g.EasyMotion_do_mapping = 0 -- Disable default mappings if you want to set your own
        end
    }
}
